function hi(name){
  console.log(name);
}

hi('world')


const bravo = require('./store');

const startingPoint = {
  x: process.env.OPENING_X,
  y: process.env.OPENING_Y;
}

const bigRival = store.familyDollar(openingDay)
console.log(bigRival);
